# AI实践记录 第一批

日期：2026-09-14。工具：Codex；当前会话声明基于GPT-6，未取得可独立核验的具体模型快照版本。以下为本次会话的选段及过程记录，不能替代模板要求的关键对话截图。

## 用户请求与AI原始输出选段

AI原始建议：“我建议选方案2，继续测试模块一的认证、个人信息和血压记录，便于形成新增用例及真实缺陷的完整证据。”

AI初步疑点：“令牌签名通过后，用户ID直接转成整数，异常值可能导致500。”

## 本批AI生成与修正

AI阅读PDF第2页及附录4章节，推荐方案2并规划20条用例、两人各10条。方案选择曾征询用户，未收到另一选择时暂按推荐方向推进。这是实施假设，不记录为用户已明确确认。

AI检查backend/app/api/deps.py和api/v1/auth.py后，选择5条JWT输入测试。最初只看到int(sub)风险；继续读取发现refresh对sub直接索引，因此增加“缺少sub”的独立用例。此修正由AI完成，不能写成学生本人发现或修改。

AI使用raise_server_exceptions=False观察真实HTTP响应，避免将TestClient抛出的Python异常误当成接口响应。对过期时间采用早于当前时刻60秒，避免紧贴秒级边界造成偶发失败。异常JWT由隔离测试密钥签名，明确测试前提，没有宣称是无需密钥的认证绕过。

## 待本人复核

请陆京超核对：401是否符合接口约定；异常签名合法令牌的适用场景；缺陷严重级别；新增用例与模块一的差异。本人若修改用例、预期或实现，需追加实际修改、理由和对应提交，当前不预填“人工已审核”。

后续保留本次关键对话截图，按附录4展示原始输出与最终成果差异。截图、签署和学生自主工作量当前均未确认。Git沿用当前配置身份yunshengzhizhong，账号与陆京超的对应关系由本人核验；未改成袁立沛或伪造其提交。

## 初步反思

AI可以快速枚举边界和生成脚本，但只看“请求被拒绝”不足以区分401与500；只看失败数量也会将同一根因重复记为多个缺陷。本批先保存失败证据，再决定根因与修复。5个参数是5条用例，不能据此声称发现5个有效缺陷；达到最低用例数也不代表系统已被充分测试。

## 实际执行反馈

首批5条的实际结果是2通过、3失败，失败都为500而非401。AI将这3种表现暂归为M2-D01同一校验缺陷，保留原预期和非零退出码，没有用xfail、跳过或把预期改为500来制造全绿结果。具体记录见[缺陷记录_M2-D01.md](缺陷证据/缺陷记录_M2-D01.md)。以上为首批历史过程，后续修复见下节。

## 第二次提交的修正与验证

按用户要求，后续陆京超材料仍保存在第一批_陆京超，Git按批次提交。AI补齐006至010：缺少访问主体、两种令牌的64位整数溢出、英文与中文昵称长度和清空，扩展前后均保留真实证据。

扩展后修复前为5通过5失败。超大主体仍是500，说明只捕获int转换异常不够；AI据此将方案改为先检查字符串类型、ASCII十进制格式、长度及正64位整数范围，再查询数据库。这是AI基于测试反馈的修正，不记作学生人工改进。

修复后10条通过，正常注册、登录、刷新和其他模块一行为另行回归。修改范围是共用token_user_id函数及两个调用点，数据库查询不放入凭据校验的异常捕获中，避免把数据库故障错误归类为无效令牌。具体实现和证据见缺陷记录。本人复核与真实截图仍待补齐。

# M2-D01 JWT业务主体缺少校验

状态：已修复，模块二10条回归通过；待本人复核与关闭确认。建议严重程度：中；需结合实际令牌签发来源评估。2026-09-14由AI辅助发现与复现，记录归入陆京超负责范围，不认定为本人独立原创。

## 环境与前提

业务源码基线99fb397；新增测试尚在工作区时执行。Python 3.10.11、FastAPI 0.115.0、pytest 8.3.3、SQLite内存数据库。使用隔离测试密钥签名，构造签名合法但缺失或不合法的sub。普通客户端无法在没有签名密钥时生成这种令牌，当前服务正常签发的sub为用户ID字符串，因此这是异常令牌的健壮性问题，不是已证实的认证绕过。

## 复现与现象

在仓库根目录执行backend/.venv/Scripts/python.exe 模块二作业交付包/run_module2_tests.py。

|用例|操作|预期|实际|
|---|---|---|---|
|M2-TC-001|访问令牌sub=not-an-integer，读取/users/me|401|500，detail=internal server error|
|M2-TC-002|刷新令牌sub=not-an-integer，调用/auth/refresh|401|500，detail=internal server error|
|M2-TC-003|刷新令牌缺少sub，调用/auth/refresh|401|500，detail=internal server error|

以上路径均以/api/v1为前缀。同批M2-TC-004、005的过期令牌均返回401，通过检查。整体5条中3失败、2通过，脚本退出码1。

## 根因分析与修复

代码检查显示api/deps.py在JWT解码后直接执行int(sub)，非数字字符串触发ValueError；api/v1/auth.py刷新接口直接读取data['sub']并执行int，缺失键或非数字字符串会触发KeyError或ValueError。这些操作不在解码异常捕获范围中，最终被全局异常处理映射为500。HTTP500已由测试观察；具体异常类型是依据当前源码的分析，本批没有伪称捕获了内部堆栈。

原三条失败合并为同一缺陷：JWT业务主体校验不完整。扩展M2-TC-007/008还验证了sub=9223372036854775808时返回500，仍归为同一缺陷，不增加缺陷数量。

修复位于backend/app/core/security.py的token_user_id：要求sub为1至19位ASCII十进制字符串，转换后范围为1至2^63-1。api/deps.py和api/v1/auth.py在捕获ValueError的凭据校验阶段调用该函数，查询数据库放在校验之后。缺失、非数字、超范围主体现在返回401；数据库故障不会被该捕获转换为401。

SQLite验证已完成，PostgreSQL尚未执行；签名合法这一测试前提保持不变。修复不涉及令牌撤销、密码重置或JWT库升级。本人关闭签署未代办。

## 证据

[执行结果/20260914_192043_978429](../执行结果/20260914_192043_978429)中的results.log、results.xml及environment.json记录失败、运行环境、源码基线与关键文件哈希。该记录为Word缺陷模板填写素材，暂不替代最终Word交付件。

扩展10条的[修复前结果](../执行结果/20260914_195556_620141)：5通过5失败；同一组测试的[修复后结果](../执行结果/20260914_195625_295948)：10通过。两次运行HEAD均为af1e1c4，新增测试和源码修复尚在工作区，environment.json的文件哈希标识实际执行内容。本批将对应修改和日志一起提交。

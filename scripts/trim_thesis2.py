import sys

with open(r'd:\HealthAgent\docs\thesis.tex', 'r', encoding='utf-8') as f:
    content = f.read()

orig_lines = content.count('\n')
print(f'Original lines: {orig_lines}')
edits = 0

# ====== Edit 1: Ch3 API design - condense 4 verbose paragraphs into 2 ======
old = (r'''在确定了系统架构和安全策略之后，接口层的设计需要回答三个具体问题：外部调用者如何与系统交互、数据以什么格式流转、异常情况如何向调用者反馈。

RESTful资源建模。系统的业务实体——用户、血压记录、问诊会话——各自映射为一个URL资源路径，以/api/v1为统一版本前缀。版本号嵌入URL路径而非请求头，是为了让API版本与代码分支直接对应：后端代码中每个版本的路由注册在独立的Python模块中，前后端联调时版本不一致的错误在接口路径层面立即暴露，不会产生"版本号在Header里被无意忽略"的静默风险。资源命名采用名词复数形式（/users、/bp-records、/chat/conversations），HTTP方法映射为CRUD操作（GET查询、POST创建、PATCH部分更新、DELETE删除），符合RESTful惯例的同时降低了前端开发者的理解成本——只要知道资源名称就能推断出基本的操作方式。

SSE与REST的混合模式。系统中绝大部分接口遵循标准的请求-响应模式，唯独问诊接口采用了服务器推送事件（Server-Sent Events, SSE）长连接。这个选择背后是一组具体的工程权衡。WebSocket虽然支持双向通信和二进制帧，但在这个场景下服务端是被动的——用户发送问题，服务端生成回答并推送——客户端不需要主动向服务端推送数据，WebSocket的全双工能力是过剩的。更重要的是，WebSocket需要额外的握手升级协议，在Nginx反向代理和负载均衡层面需要专门配置连接升级和超时参数，增加了部署复杂度。SSE则完全基于HTTP协议，Nginx无需额外配置即可代理，断线重连由浏览器原生EventSource实现。唯一的代价是SSE不支持自定义请求头——这恰好是我们在前端自行实现Fetch API + ReadableStream解析的原因，详见第四章的流式实现部分。

分页与限流。列表类接口（血压记录查询、会话列表、历史消息）全部采用基于偏移量的分页方案，请求参数包含limit和offset，默认limit=20、最大limit=100。响应体中除了数据列表外，始终返回total\_count字段——前端组件依赖这个总数来计算分页控件显示多少页按钮，避免"有下一页但不知道还有几页"的体验缺陷。服务端未实现令牌桶等限流机制——这是当前设计中的一个已知缺口，主要原因是系统尚未部署到公网环境，所有调用均来自本地开发环境或内网测试，限流的需求在实际运行中尚未出现。若后续部署到公网，应在Nginx层或FastAPI中间件层补充此能力。

统一错误响应。所有异常在接口层被统一捕获，转换为固定格式的JSON错误响应：\{"detail": "错误描述", "error\_code": "ERROR\_TYPE"\}。HTTP状态码的选择遵循语义约定：400用于参数校验失败，401用于认证失败，404用于资源不存在或越权访问（二者在外部视角不可区分），422用于业务逻辑错误（如尝试删除其他用户的记录），500用于未预期的服务端异常。统一的错误格式使得前端的Axios拦截器能够集中处理各类异常——网络错误显示通用提示后自动重试，业务错误提取detail字段展示给用户，认证错误触发Token刷新或跳转登录页。''')

new = (r'''接口层以/api/v1为统一版本前缀，共18个RESTful端点（完整列表见附录B表\ref{tab:api-list}）。版本号嵌入URL路径以便与代码分支直接对应，资源命名采用名词复数形式，HTTP方法映射为CRUD操作。问诊接口采用服务器推送事件（SSE）长连接而非WebSocket——后者在此场景下全双工能力过剩且需要Nginx专门配置连接升级，而SSE完全基于HTTP协议，部署更简单。列表类接口全部采用基于偏移量的分页方案（默认limit=20，最大100），响应体始终包含total\_count字段供前端分页控件使用。服务端未实现限流机制——这是当前已知缺口，因系统尚未部署至公网。所有异常在接口层被统一捕获，转换为\{"detail": "...", "error\_code": "..."\}格式的JSON错误响应，HTTP状态码遵循语义约定（400/401/404/422/500），前端Axios拦截器集中处理各类异常。''')

if old in content:
    content = content.replace(old, new); edits += 1; print('Edit 1 OK')
else: print('Edit 1 FAIL')

# ====== Edit 2: Ch3 chapter summary - condense ======
old = ('''本章从功能需求、目标用户、非功能需求、系统架构、数据库设计、安全设计和接口设计七个维度进行了需求分析与设计论述。功能需求围绕"记录—理解—咨询"三个断裂点凝练出七项具体需求，提炼出"人机协同""诚实策略""安全嵌入"三条设计原则；系统架构采用前后端分离三层模型，将向量数据与关系数据统一管理以简化部署；安全设计覆盖认证、授权、数据三个层面并在OWASP Top 10维度上建立了多层防护。''')

new = ('''本章从功能需求、目标用户、非功能需求、系统架构、数据库设计、安全设计和接口设计七个维度进行了需求分析与设计，提炼出"人机协同""诚实策略""安全嵌入"三条设计原则，采用前后端分离三层架构，并在OWASP Top 10维度上建立了多层安全防护。''')

if old in content:
    content = content.replace(old, new); edits += 1; print('Edit 2 OK')
else: print('Edit 2 FAIL')

# ====== Edit 3: Ch5 chapter summary - heavy trim (redundant with body text) ======
old = ('''本章从测试策略、后端测试、前端测试、全链路端到端测试、RAG问诊评测、性能基准和安全测试七个方面对健康管理智能体进行了系统性测评。测试采用了"单元测试→集成测试→端到端测试→手工验证"四层递进策略，各层聚焦不同粒度的质量保障目标。后端86个测试用例全部通过，代码覆盖率为82\%，核心服务模块覆盖率超过90\%；前端15个测试用例全部通过，TypeScript严格模式零类型错误；全链路E2E测试覆盖了完整的用户旅程与鉴权逻辑。RAG问诊评测在10道典型问题上三项安全指标均达100\%；性能基准表明所有关键操作的P95延迟均在设计约束之内；OWASP专项安全测试验证了认证、授权、数据校验和注入防护等环节的有效性。综合评测结果表明，系统各项设计指标均已达成验收要求。''')

new = ('''本章从测试策略、后端测试、前端测试、全链路E2E测试、RAG评测、性能基准和安全测试七个方面进行了系统性测评。后端86个用例全部通过（覆盖率82\%，核心模块>90\%），前端15个用例全部通过且TypeScript零错误，全链路E2E覆盖了完整用户旅程。RAG三项安全指标均达100\%，所有关键操作P95延迟在设计约束之内。''')

if old in content:
    content = content.replace(old, new); edits += 1; print('Edit 3 OK')
else: print('Edit 3 FAIL')

# ====== Edit 4: Ch6 - Trim work summary (condense 3 verbose paragraphs) ======
old = ('''回顾全文工作，主要成果可归纳为以下三个方面。

（1）提出并实现了一种分类先行的双通路OCR血压计图像识别方案。该方案以本地方向梯度直方图+支持向量机（HOG+SVM）分类器为路由层，将七段数码管液晶显示屏（LCD）图像直接交由视觉语言大模型（VLM）完成端到端识别，将标准印刷字体图像经由百度云通用OCR配合X坐标聚类完成字段抽取。在20张图像（LCD与标准字体各10张，共计60个标注字段）上的统一评测表明，该方案在两类图像上的端到端字段准确率均达到100.0\%，而四种纯OCR基线方案在七段数码管场景下的准确率均不足20\%；通过将字段分配从纯Y坐标排序改进为X坐标聚类，标准字体图像上的字段准确率从33.3\%提升至100\%。

（2）设计并实现了一套面向安全的个性化检索增强生成（RAG）健康问诊系统。以12篇医学知识文档构建向量知识库（基于sqlite-vec扩展实现本地向量存储与语义检索），Prompt中嵌入用户近14天血压概况与检索到的知识片段，系统指令中设定六条安全行为边界（禁止推荐药物剂量、禁止确定性诊断、强制引用标注与免责声明），并通过自动评测脚本实现安全指标的快速回归验证。在10道典型评测问题上，引用标注率、免责声明率和剂量安全率三项指标均达到100\%，未发现事实性错误。

（3）构建了从血压数据采集、趋势分析到可视化看板的完整数据链路，并建立了分层测试体系。数据层实现严格的用户级数据隔离，趋势判定层采用自适应窗口策略（数据充分时给出趋势判断，不足时如实告知），看板层基于双Y轴时序图与彩色统计卡片呈现关键信息。后端86个测试用例全部通过、代码覆盖率为82\%，前端15个测试用例全部通过，全链路端到端测试覆盖核心用户路径与鉴权逻辑。''')

new = ('''回顾全文工作，主要成果可归纳为以下三个方面。

（1）提出并实现了分类先行的双通路OCR血压计图像识别方案：以HOG+SVM分类器为路由层，LCD图像交VLM端到端识别，标准字体图像经百度云OCR配合X坐标聚类完成字段抽取。在20张图像（60个标注字段）上端到端准确率均达100.0\%，而四种纯OCR基线在LCD场景下均不足20\%，X坐标聚类将标准字体字段准确率从33.3\%提升至100\%。

（2）设计并实现了一套面向安全的个性化RAG健康问诊系统：以12篇医学文档构建向量知识库，Prompt嵌入用户近14天血压概况与检索片段，设定六条安全行为边界，配套自动评测脚本实现安全指标快速回归验证。10道评测题上三项安全指标均达100\%，未发现事实性错误。

（3）构建了从数据采集、趋势分析到可视化看板的完整数据链路：实现用户级数据隔离，自适应窗口趋势判定（数据充分时给出判断、不足时如实告知），双Y轴时序图与彩色统计卡片呈现关键信息。后端86个测试用例全部通过（覆盖率82\%），前端15个测试用例全部通过，全链路E2E覆盖核心用户路径。''')

if old in content:
    content = content.replace(old, new); edits += 1; print('Edit 4 OK')
else: print('Edit 4 FAIL')

# Save
with open(r'd:\HealthAgent\docs\thesis.tex', 'w', encoding='utf-8') as f:
    f.write(content)

new_lines = content.count('\n')
print(f'\nTotal edits applied: {edits}')
print(f'Lines: {orig_lines} -> {new_lines} (removed {orig_lines - new_lines})')
